import RegisterForm from "@/components/RegisterForm";

const RegisterPage = () => {
  return (
    <div className="p-3 flex items-center justify-center h-auto shadow-md lg:w-[40%] lg:ml-auto lg:overflow-y-scroll bg-white">
      <RegisterForm />
    </div>
  );
};

export default RegisterPage;
